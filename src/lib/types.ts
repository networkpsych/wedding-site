export type CustomImage = {
    uri: string;
    desc: string;
};

export type NavLinks = {
    name: string
    link: string
}

export type AboutLinks = {
    image: string;
    alt: string;
    topic: string;
    desc: string;
    url: string;
}

export type CDTimer = {
    id: string;
    time: number;
}